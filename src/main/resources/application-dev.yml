server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/visceral
    username: visceral
    password: visceral
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

jwt:
  secret: change-me
  accessTtlMinutes: 15
  refreshTtlDays: 30